#include <stdio.h>
#include <string.h>

int main() {
    char s[1000];
    scanf("%s",s);
    int l=strlen(s),p1=0,p2=0;
    for(int i=0;i<l;i++){
        if(s[i]=='+')
        {
            p1++;
        }
        else
        {
            p2++;
        }
    }
    printf("%d",abs(p1-p2));
}
