#include <stdio.h>
#include<string.h>

int main() {
   char s[100];
   scanf("%s",s);
   int l=strlen(s),i,c=0;
   for(i=0;i<l;i++)
   {
       if(s[i]==s[l-i-1])
       {
           c++;
       }
   }
   if(l==c)
   {
       printf("YES");
   }
   else
   {
       printf("NO");
   }
}
