#include <stdio.h>

int main() {
    int a,b,c,f=1;
    scanf("%d %d",&a,&b);
    int lcm=(a>b) ? a : b;
    while(f)
    {
        if(lcm%a==0 && lcm%b==0)
        {  
            printf("%d",lcm);
            f=0;
        }
        lcm++;
    }
    return 0;
}
