#include <stdio.h>

int main() {
   char s[100];
   scanf("%s",&s);
   int l=strlen(s);
   int c=0;
   for(int i=0;i<l;i++)
   {
       if(s[i]==s[l-i-1])
       {
           c++;
       }
   }
   if(c==l)
   printf("palindrome");
   else
   printf("not palindrome");

    return 0;
}
